const getFullName = (firstName, lastName) => firstName + ' ' + lastName;
const getSurname = (name, surname) => surname; //Local variable

export default {getFullName, getSurname};


export const getFullName_ = (firstName, lastName) => firstName + ' ' + lastName;
export const getSurname_ = (name, surname) => surname;
